fix: stabilize seed migrations and align backend with frontend contract

Fixes seed migration inconsistency that caused different behavior across environments:
- Source migration V1__seed_realistic_test_data.sql added to db/seeds for reproducibility
- Password migration V2__seed_passwords_and_dev_alignment.sql with crypt() for dev login
- Flyway dev strategy updated with repair+migrate for checksum alignment
- Backend startup diagnostics logger added (active profile, datasource URL, migration history, data counts)

Implements missing core API endpoints:
- GET /api/applications/board - returns applications grouped by status (Kanban view)
- GET /api/analytics/summary - returns conversion funnel and activity metrics

Fixes and improvements:
- Lazy-loading crash in vacancies mapper resolved
- Temporary diagnostic logging added to list methods (companies, vacancies, applications)
- Debug endpoint GET /api/users added (dev profile only)
- Frontend mock fallback corrected to backend-first (VITE_USE_MOCKS defaults to false)
- .env.example documentation updated with correct defaults

Benefits:
- Consistent behavior between local dev and CI/test environments
- Seed data now properly version-controlled and reproducible
- Developers can log in with working demo credentials
- Frontend applications/analytics pages now work against real backend
- Application board Kanban and analytics dashboard fully functional

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
