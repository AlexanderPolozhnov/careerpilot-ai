# CareerPilot AI — SWE & Tech Lead Collaboration Workflow

Этот файл описывает процесс взаимодействия между Архитектором (Gemini) и Исполнителем (SWE-1.6 в Windsurf).

```
Для ручного тестирования всего приложения запустить в корне проекта в терминале ./verify-all.ps1
```
---

## 1. Анализ архитектуры (Техлид / Gemini)
```
Прочитай эти файлы перед началом работы:
   - GEMINI.md — твоя роль и правила.
   - Раздел "⚠️ Известные ошибки и паттерны" в GEMINI.md, полезно как для тебя, так и про проверке на review.
   - docs/CAREERPILOT_AI_CONTEXT_BACKUP.md — реальный статус проекта (раздел ## Update)
   - ROADMAP.md — план, что сделано и что нет
   - docs/FRONTEND_BACKEND_CONTRACT.md — API контракт
                                                                                                      
   После прочтения скажи кратко: какие слайсы реализованы и какая следующая задача по ROADMAP. Если после задачи или щас нужно делать релиз, описываешь его красивый текст и наименование, тег для github, если нет то пропусти
```
---

## 2. Анализ (Техлид)
```
Задача: [ОПИСАНИЕ ЗАДАЧИ], выбери сам более предпочтительную по приоритету.
                                                                                                      
   Прочитай файлы:
   - backend/src/main/java/... (найди похожий реализованный слайс для понимания паттернов)
   - docs/FRONTEND_BACKEND_CONTRACT.md (секция с нужным endpoint)
   - docs\TASK_PLAN.md (шаблон задачи из GEMINI.md)
                                                                                                      
   Реализуй: только план, не код.
   Создай docs/tasks/new_tasks/TASK_{NAME}.md по шаблону из GEMINI.md.
```
---

## 3. Запуск SWE-1.6 в Windsurf (Исполнитель)
```
Прочитай и реализуй задачу по плану:


Дополнительный контекст:
 @GEMINI.md (ОБЯЗАТЕЛЬНО прочитай раздел "⚠️ Известные ошибки и паттерны (Lessons Learned для SWE-1.6)" перед началом работы!)
 @docs/FRONTEND_BACKEND_CONTRACT.md @docs/CAREERPILOT_AI_CONTEXT_BACKUP.md (прочитай ## Update блоки для понимания текущего состояния)

Следуй порядку реализации из плана.
Не меняй файлы вне scope задачи.
ОБЯЗАТЕЛЬНО перед завершением выполни локальную валидацию через .\verify-all.ps1 в корне проекта. 
Если скрипт выдает ошибки — исправляй их! Пуш или отчет без успешной валидации ЗАПРЕЩЕН.
После реализации выполни проверки из раздела "Проверки" и учет раздела "⚠️ Известные ошибки и паттерны" в GEMINI.md чтобы не было ошибок.
В самом конце отчета предоставь полный список всех путей к файлам (от корня проекта), которые были созданы или изменены.
Выполни первичную синхронизацию всех связанных документов (ROADMAP.md, CAREERPILOT_AI_CONTEXT_BACKUP.md, DEPLOYMENT.md, README.md, README.DEV.md), отразив в них внесенные изменения.
В конце если были ошибки или нюансы — обнови раздел "⚠️ Известные ошибки и паттерны" в GEMINI.md, но только если ты точно уверен что решение правильное и сооветствует best practics.
Обязательно не забывай про i18n ключи!
```
---
## 4. Review code и синхронизация (Tech Lead Gemini)
```
SWE-1.6 завершил задачу.
                                                                                                      
   Проведи code review как tech lead (в отчете SWE ниже указан список затронутых файлов):
   1. Всё ли соответствует плану TASK_PLAN.md?
   2. Соблюдены ли архитектурные правила из GEMINI.md?
   3. Пройдена ли ОБЯЗАТЕЛЬНАЯ валидация .\verify-all.ps1 без ошибок?
   4. Проверь синхронизацию ROADMAP.md, README.md, README.DEV.md и docs/CAREERPILOT_AI_CONTEXT_BACKUP.md, выполненную SWE (если требуется).
   5. Если были ошибки или нюансы — обнови раздел "⚠️ Известные ошибки и паттерны" в GEMINI.md.     
   6. Дай гайд, как мне проверить изменения вручную на сайте (как пользователю и как разработчику).   
                                                                                                      
   Вот его отчет:
```
---
## 5. Гит коммит и пуш (Исполнитель)
```
   Сделай коммит всех git status изменений, а я сам сделаю пуш в репозиторий.
   ВНИМАНИЕ: Git автоматически запустит .\verify-all.ps1 перед пушем. Если там будут ошибки, пуш будет отменен.

   Правила для коммита:
   - Сообщение в прошедшем времени безличной форме (добавлено, обновлено, изменено, исправлено и т.д.)
   - Структурируй по категориям (Backend, Frontend, Документация, Конфигурация и т.д.)
   - Не перечисляй каждый файл, описывай общие изменения по категориям
   - Используй префикс conventional commits: feat:, fix:, refactor:, docs:, chore:, test:
```

## 🔍 Гайд по ручной проверке (Manual Testing)
  Как запустить:
   1. Запусти Docker контейнеры (PostgreSQL, Redis) если не запущено локально: docker compose up -d
```
docker compose up -d
```
   2. Запусти backend: в папке backend выполни .\mvnw.cmd spring-boot:run
```
.\mvnw.cmd spring-boot:run
```
   3. Запусти frontend: в папке frontend выполни npm run dev
```
npm run dev
```
