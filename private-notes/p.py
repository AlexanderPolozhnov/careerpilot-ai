import sys
import pyperclip
import os

PROMPTS = {
    "1": """Прочитай эти файлы перед началом работы:
- GEMINI.md — твоя роль и правила
- docs/CAREERPILOT_AI_CONTEXT_BACKUP.md — реальный статус проекта (раздел ## Update)
- ROADMAP.md — план, что сделано и что нет
- docs/FRONTEND_BACKEND_CONTRACT.md — API контракт

После прочтения скажи кратко: какие слайсы реализованы и какая следующая задача по ROADMAP.""",

    "2": """Задача: [ОПИСАНИЕ ЗАДАЧИ], выбери сам более предпочтительную по приоритету.

Прочитай файлы:
- backend/src/main/java/... (найди похожий реализованный слайс для понимания паттернов)
- docs/FRONTEND_BACKEND_CONTRACT.md (секция с нужным endpoint)

Реализуй: только план, не код.
Создай docs/tasks/TASK_{NAME}.md по шаблону из GEMINI.md.""",

    "3": """Прочитай и реализуй задачу по плану:
@docs/tasks/TASK_{NAME}.md

Дополнительный контекст:
@GEMINI.md (ОБЯЗАТЕЛЬНО прочитай раздел \"⚠️ Известные ошибки и паттерны (Lessons Learned для SWE-1.6)\" перед началом работы!)
@docs/FRONTEND_BACKEND_CONTRACT.md

Следуй порядку реализации из плана. Не меняй файлы вне scope.
После каждого шага кратко пиши, что сделал.
Выполни проверки из раздела \"Проверки\" в конце.""",

    "4": """SWE-1.6 завершил задачу. Вот его отчет:
[ВСТАВИТЬ ТЕКСТ ОТЧЕТА ИЗ WINDSURF]

Проведи code review:
1. Всё ли соответствует плану TASK_PLAN.md?
2. Соблюдены ли архитектурные правила из GEMINI.md?
3. Синхронизируй ROADMAP.md и docs/CAREERPILOT_AI_CONTEXT_BACKUP.md.
4. Если были ошибки или нюансы — обнови раздел \"⚠️ Известные ошибки и паттерны\" в GEMINI.md.
5. Дай гайд, как мне проверить изменения вручную на сайте (как пользователю и как разработчику).""",

    "5": """Подготовь сообщение для коммита согласно стилю проекта.
Проверь git status и текущие изменения в файлах."""
}

def main():
    if len(sys.argv) < 2:
        print("Использование: python prompt.py [номер промта (1-5)]")
        return

    num = sys.argv[1]
    if num in PROMPTS:
        pyperclip.copy(PROMPTS[num])
        print(f"✅ Промт №{num} скопирован в буфер обмена!")
    else:
        print("❌ Неверный номер. Используйте 1, 2, 3, 4 или 5.")

if __name__ == "__main__":
    main()
