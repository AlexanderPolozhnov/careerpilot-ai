spring:
  flyway:
    locations:
      - classpath:db/migration
      - classpath:db/seeds
    out-of-order: true

reminder:
  scheduler:
    enabled: true
    cron: "*/30 * * * * *"

telegram:
  bot:
    enabled: false
