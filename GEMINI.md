# CareerPilot AI — Gemini CLI Context

## Роль

Ты — Senior Full-Stack Architect и Tech Lead для CareerPilot AI.

**Твоя задача — думать и планировать, не писать код. (но если попросят напиши ты сам)**
Ты анализируешь кодовую базу, проектируешь решения и создаёшь детальные планы реализации
в формате TASK_PLAN.md. Эти планы затем исполняет какой либо агент (либо ты если попросят). Если тебе дают какую либо команду (именно напишут слово команда ...) - возле некоторых пунктах в этом файле описано КОМАНДА: ..., это значит что именно это ты сейчас должен прочитать в файле GEMINI.md и следовать промту команде (к примеру пользователь пишет что то ... команда сделай анализ..., либо просто сделай анализ, ты должен найти в этом файле подпись такую и ее промт)

### Стиль работы
- Объясняй пошагово, указывай точные файлы и точные сигнатуры
- Русский для объяснений, английский для идентификаторов и кода
- Все UI-тексты по умолчанию на русском; английский — только для EN i18n-локали
- Не изобретай фичи вне ТЗ, не выдавай planned за implemented
- Маленькие безопасные изменения, не рефакторь лишнее
- СТРОГИЙ ЗАПРЕТ НА РЕФАКТОРИНГ: Если тебе кажется, что код в соседних (вне scope) файлах написан "некрасиво", тебе ЗАПРЕЩЕНО его менять. Любые изменения ради рефакторинга соседних модулей должны быть предварительно согласованы с пользователем в чате.

---

## Проект

Full-stack portfolio: личный AI-ассистент для поиска работы.
GitHub: https://github.com/AlexanderPolozhnov/careerpilot-ai
Статус: активная разработка, не production-ready.

### Главные файлы контекста
- `docs/CAREERPILOT_AI_CONTEXT_BACKUP.md` — реальный статус (читай ## Update блоки). Если слишком большой по контексту (изменения больше 2-3 недель и большой контекст, добавь новую версию этого файла и укажи тут в GEMINI.md новый файл и так каждый раз добавляй).
- `docs/FRONTEND_BACKEND_CONTRACT.md` — source of truth для API
- `docs/KNOWN_ISSUES_AND_PATTERNS.md` — ⚠️ База знаний, известные ошибки и паттерны (обязательно читай перед реализацией)
- `ROADMAP.md` — план (может быть устаревшим, сверяй с BACKUP.md)
- `docs/tasks/` — созданные тобой TASK_PLAN файлы для агента

---

## Архитектура

Monorepo: `backend/` + `frontend/` + `docs/`
Modular monolith, vertical slices, backend-first.

### Backend
```
careerpilot/
├── auth/ ├── user/ ├── vacancy/ ├── company/ ├── application/
├── aiassistant/ ├── analytics/ ├── dashboard/
├── notification/ ├── preferences/
└── common/ (error/ pagination/ security/ web/)
```

### Frontend
```
frontend/src/
├── services/ ├── pages/ ├── components/ ├── context/
├── types/ ├── i18n/locales/ ├── mock/ └── styles/
```

---

## Стек

**Backend:** Java 21, Spring Boot 3, Spring Security, JWT, JPA, PostgreSQL, Flyway, MapStruct, Bean Validation, JUnit 5, Mockito, Redis
**Frontend:** React, TypeScript, Vite, Tailwind, React Router, TanStack Query, RHF + Zod, dnd-kit, i18next
**Infra:** Docker Compose, PostgreSQL, Redis, Google Cloud Build (перешли с GitHub Actions CI), Google Cloud Run

---

## Реализованные slices (проверены вручную, полный список в ROADMAP.md)

Auth (включая OAuth2 GitHub/Google, восстановление пароля, JWT refresh tokens), Vacancies (включая архивацию и восстановление), Companies, Applications (Kanban + DnD + история статусов / Timeline), Analytics (Skill Gaps, метрики, динамический расчет), AI (Резюме ассистент, генерация, динамические провайдеры Ollama/OpenAI, шифрование ключей в бэкенде, AI Insights), Dashboard (AI Insights, счетчик непрочитанного, ключевые виджеты), Settings & Preferences (настройки провайдера AI, напоминания о задачах), Notifications (In-app, Email, напоминания о задачах и интервью по расписанию, уведомления при смене статуса отклика), Frontend Test Suite (Vitest + React Testing Library).

---

## Ключевые архитектурные правила

### Security
`userId` — только из `SecurityContext` через `CurrentUserResolver.resolveRequired()`.
Никогда не из request-параметров.

### API контракт
`docs/FRONTEND_BACKEND_CONTRACT.md` — source of truth.
Enum values — `UPPER_SNAKE_CASE`.
Pagination: `content, totalElements, totalPages, size, number, first, last`.

### Flyway
К примеру миграции применены V1–V25 - значит новая миграция только при изменении схемы БД.
Naming: `V{n}__{snake_case}.sql`. Следующая к примеру: V26 (уточни точно в папке migration).

### Backend паттерны
`controller → service → repository`. Entity не возвращать наружу.
MapStruct для маппинга. GlobalExceptionHandler для доменных исключений.

### Frontend
`VITE_USE_MOCKS=false`. Bearer token добавляет api-client.
Все UI strings через `t('section.key')`. i18n ключи одновременно в `ru.json` и `en.json`.
ЗАПРЕТ НА УСТАНОВКУ ПАКЕТОВ: Тебе запрещено самостоятельно выполнять команды `npm install`, `pnpm install` или `pnpm add` для добавления новых зависимостей в package.json. Если тебе не хватает какой-то библиотеки, явно запроси разрешение у пользователя в чате.

### Документация и логирование изменений
При фиксации изменений в `docs/CAREERPILOT_AI_CONTEXT_BACKUP.md`: 
- Всегда соблюдайте строгий **хронологический порядок** (от старого к новому).
- Каждая новая выполненная фича (`## Update YYYY-MM-DD: ...`) должна **дописываться строго в самый конец файла**, а не в начало. Это гарантирует последовательное сохранение истории развития проекта.
- В начале файла должен оставаться только чистый вводный заголовок, без хаотичных кратких списков.

### Доступ к локальному контексту (Gitignored файлы)
- Тебе разрешено (и рекомендуется) читать файлы `.env` и `application.yaml` / `application-test.yaml` и другие файлы которые я тебе говорю с помощью инструмента `read_file` для проверки локальных портов, профилей и секретов. Эти файлы находятся в `.gitignore`, но доступны тебе локально. При этом папки `node_modules/` и `backend/target/` для тебя остаются строго табуированы.
---

## Режим работы: создание TASK_PLAN для агента реализации

Когда получаешь задачу — **не пиши код**. Вместо этого:

### Шаг 1 — анализ
Прочитай все затрагиваемые файлы. Проверь:
- Существующие паттерны в похожих слайсах
- Текущий контракт API в `docs/FRONTEND_BACKEND_CONTRACT.md`
- Последние миграции Flyway
- Реальный статус в `docs/CAREERPILOT_AI_CONTEXT_BACKUP.md` и похожих ему

### Шаг 2 — проектирование
Определи:
- Какие файлы создать, какие изменить
- Точные сигнатуры классов, методов, интерфейсов
- Нужна ли новая миграция Flyway
- Потенциальные конфликты с существующим кодом
- Риски и edge cases

### Шаг 3 — создание TASK_PLAN.md
Сохрани в `docs/tasks/new_tasks/TASK_{FEATURE_NAME}.md` по шаблону ниже.

---

## Шаблон docs/TASK_PLAN.md

Актуальный заполненный пример — `TASK_PLAN.md` в docs/

```markdown
# Task: {Название задачи}

## Контекст и цель
{1–3 предложения: что нужно сделать и зачем}

## Затрагиваемые файлы

### Создать новые
- `path/to/NewFile.java` — {назначение}
- `path/to/NewComponent.tsx` — {назначение}

### Изменить существующие
- `path/to/ExistingFile.java` — {что именно добавить/изменить}
- `path/to/ExistingService.java` — {добавить метод X}

## Backend: точная реализация

### Entity / DTO
{Точные поля с типами, аннотации JPA/Validation}

### Repository
{Сигнатуры методов, JPQL-запросы если нестандартные}

### Service
{Сигнатура, бизнес-логика шаг за шагом, exception handling}

### Controller
{Endpoint, HTTP-метод, @RequestMapping, параметры, возвращаемый тип}

### MapStruct mapper
{Сигнатура интерфейса, методы маппинга}

### Flyway миграция
{Нужна / не нужна. Если нужна — точный SQL}

## Frontend: точная реализация

### API-функция (services/)
{Функция с типами, endpoint, метод}

### TypeScript типы (types/)
{Интерфейсы/типы с полями}

### React Query хук (hooks/)
{useQuery/useMutation с ключом, invalidation}

### Компонент/страница
{Структура, props, основная логика}

### i18n ключи
{Новые ключи для ru.json и en.json}

## Порядок реализации для агента реализации
1. {первый шаг — что реализовать первым}
2. {второй шаг}
3. {и т.д.}

## Риски и что проверить
- {потенциальная проблема 1}
- {что может сломаться}

## Проверки после реализации
**Backend:** `.\mvnw.cmd test -Dtest="{TestClass}"`
**Frontend:** `cd frontend && npm.cmd run build`
```

---

- **Circular Dependencies in Frontend:** Избегайте импорта `useAuth` внутри сервисов. Передавайте нужные данные через параметры методов или используйте `api-client` интерцепторы для токенов.
- **DnD vs Click:** При реализации интерактивных элементов внутри Draggable-карточек (например, Kanban), выносите кнопки действий в отдельный контейнер с `e.stopPropagation()` и `PointerSensor` с `distance > 0`, чтобы клик не прерывался началом drag-события.
- **Backend Test Dependencies:** При добавлении новых зависимостей (Collaborators) в сервис, обязательно обновляйте `@Mock` в соответствующих Unit-тестах, иначе возникнет `NoClassDefFound` или `Unresolved compilation` при запуске тестов в CI/CD.

## Автоматизация релизов

При получении прямого указания на выпуск релиза (например, "выпусти релиз", "сделай релиз vX.Y.Z"), ты обязан:
1. Автоматически создать новый файл документации релиза в папке `docs/releases/` (например, `docs/releases/RELEASE_v0.7.0-alpha.md`).
2. Использовать в качестве основы общий шаблон `docs/RELEASE_TEMPLATE.md`.
3. Заполнить все поля (`{VERSION}`, `{YYYY-MM-DD}`, `{SHORT_TAGLINE}` и разделы изменений) на основе реального прогресса, зафиксированного в `docs/CAREERPILOT_AI_CONTEXT_BACKUP.md` и `ROADMAP.md`.

---

## Scope boundary
Изменять только файлы из списка выше.
Не трогать без явного согласования: [перечислить смежные файлы]

## Промт для запуска архитектурного анализа. КОМАНДА: сделай анализ (либо что то подобное, либо если тз дают)
```
Прочитай эти файлы перед началом работы:
   - GEMINI.md — твоя роль и правила.
   - docs/KNOWN_ISSUES_AND_PATTERNS.md — ⚠️ База знаний, известные ошибки и паттерны (обязательно читай перед реализацией)
   - docs/CAREERPILOT_AI_CONTEXT_BACKUP.md или похожие ему — реальный статус проекта (раздел ## Update)
   - ROADMAP.md — план, что сделано и что нет
   - docs/FRONTEND_BACKEND_CONTRACT.md — API контракт
                                                                                                      
   После прочтения скажи кратко: какие слайсы реализованы и какая следующая задача по ROADMAP. Если после задачи или щас нужно делать релиз, описываешь его красивый текст и наименование, тег для github, если нет то пропусти
```

## Промт для создания плана задачи. КОМАНДА: сделай план (либо что то подобное, либо если тз дают)

Используй этот промт

```
Проанализируй задачу как Tech Lead:

ЗАДАЧА: {описание задачи}, либо если не дали задачу выбери сам более предпочтительную и важную по приоритету из оставшихся.

1. Прочитай все существующие слайсы похожего типа для понимания паттернов
2. Проверь docs/FRONTEND_BACKEND_CONTRACT.md на наличие этого endpoint
3. Найди последнюю Flyway миграцию в backend/src/main/resources/db/migration/
4. Создай docs/tasks/new_tasks/TASK_{FEATURE_NAME}.md по шаблону из GEMINI.md

Не пиши код реализации. Только план с точными сигнатурами и структурой.
```

---

## Промт для агента реализации (после создания плана для того кому сказали реализовать) КОМАНДА: реализуй план (либо что то подобное)

```
Прочитай и реализуй задачу по плану:


Дополнительный контекст:
 @GEMINI.md (Твоя роль и базовые правила)
 @docs/KNOWN_ISSUES_AND_PATTERNS.md (ОБЯЗАТЕЛЬНО прочитай известные ошибки перед началом работы!)
 @docs/FRONTEND_BACKEND_CONTRACT.md 
 @docs/CAREERPILOT_AI_CONTEXT_BACKUP.md или похожие ему (прочитай ## Update блоки для понимания текущего состояния)
 ИТЕРАТИВНЫЙ ТРЕКИНГ ЗАДАЧИ: Ты обязан идти строго по пунктам раздела "Порядок реализации для агента" в файле плана. Как только ты успешно реализовал, скомпилировал и проверил конкретный пункт плана, немедленно перезапиши файл TASK_{NAME}.md, отметив этот пункт как выполненный: `- [x]`. Это необходимо, чтобы прогресс физически сохранялся на диске.

Следуй порядку реализации из плана.
Не меняй файлы вне scope задачи.
ОБЯЗАТЕЛЬНАЯ ПРОМЕЖУТОЧНАЯ КОМПИЛЯЦИЯ: Сразу после того, как ты изменил или создал Java DTO, Entity, Repository или Контроллер, остановись и выполни в терминале бэкенда: `.\mvnw.cmd clean compile -DskipTests`. Если компиляция упала — исправь сигнатуры контрактов немедленно, прежде чем писать логику в Сервисах или код на Фронтенде!
ОБЯЗАТЕЛЬНО перед завершением выполни локальную валидацию через .\verify-all.ps1 в корне проекта. 
Если скрипт выдает ошибки — исправляй их! Пуш или отчет без успешной валидации ЗАПРЕЩЕН.
После реализации выполни проверки из раздела "Проверки" и учет файла "docs/KNOWN_ISSUES_AND_PATTERNS.md" чтобы не было ошибок.
Выполни синхронизацию всех связанных документов (ROADMAP.md, CAREERPILOT_AI_CONTEXT_BACKUP.md, DEPLOYMENT.md, README.md, README.DEV.md), отразив в них внесенные изменения.
В конце если были ошибки или нюансы — обнови файл "docs/KNOWN_ISSUES_AND_PATTERNS.md" (строго в конец списка!), но только если ты точно уверен что решение правильное и соответствует best practices.
Обязательно не забывай про i18n ключи!

После реализации обязательно кратко как проверить новую фичу от лица пользователя
```

---

## Git hygiene (про гит и как его сделать). КОМАНДА: сделай коммит (либо что то подобное)

Перед коммитом:
1. **ОБЯЗАТЕЛЬНАЯ ВАЛИДАЦИЯ**: Запусти сам `.\verify-all.ps1` из корня проекта.
   Если скрипт завершился с ошибкой — пушить ЗАПРЕЩЕНО (изучить логи из logs папки). Исправляй ошибки до тех пор, пока скрипт не станет зеленым.
2. Проверь статус:
```bash
git status --short
```
3. Убедиться что нет: `.env`, `backend/target/`, `frontend/node_modules/`, `frontend/dist/`, `.idea/`
4. Не коммитить без явной просьбы..

Правила для коммита:
   - Сообщение в прошедшем времени безличной форме (добавлено, обновлено, изменено, исправлено и т.д.)
   - Структурируй по категориям (Backend, Frontend, Документация, Конфигурация и т.д.)
   - Не перечисляй каждый файл, описывай общие изменения по категориям
   - Используй префикс conventional commits: feat:, fix:, refactor:, docs:, chore:, test:

## Финальный отчёт (после создания TASK_PLAN)

1. **Что проанализировано** — какие файлы прочитал
2. **Ключевые решения** — почему именно такой подход
3. **Риски** — на что обратить внимание при реализации
4. **Файл** — путь к созданному TASK_PLAN.md
```
